import { expect } from "chai"

describe("GetAuthTokenUserUseCase#execute", function () {
  let authToken = ""
  it("gets an auth token based on provided apiKey", async function () {
    expect(authToken).not.to.be.undefined
  })
})
